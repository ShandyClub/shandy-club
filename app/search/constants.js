export const name = 'search'

// default search coordinates
export const DEFAULT_POINT = [ -0.127694, 51.507463 ]

// max distance between location and pub (in meters -> 1 mile === ~1600 meters)
export const MAX_DISTANCE = 1600
